// ⚠️ DANGER — เปิดอ่าน/เขียนได้ทุกคน แม้ไม่ล็อกอิน
// ใช้เฉพาะทดสอบชั่วคราว แล้วรีบเปลี่ยนกลับทันที
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
