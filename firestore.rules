rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // เปิดทุกอย่างถึงวันที่กำหนด (แก้วันตามต้องการ)
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2025, 8, 21);
    }
  }
}
